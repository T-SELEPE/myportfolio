import React from "react";
import "./index.scss";

const Dashboard = () => {
    return (
        <div className="dashboard-page">
            <h1>Dashboard</h1>
            <p>This is a placeholder dashboard component.</p>
            <p>Firebase functionality has been removed as per request.</p>
        </div>
    );
}

export default Dashboard;